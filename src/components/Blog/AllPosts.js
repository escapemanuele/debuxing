import React from 'react'
import { Section, Title } from '../../utils';

const AllPosts = () => {
    return (
        <Section>
            <Title title="all title" />
        </Section>
    )
}

export default AllPosts
